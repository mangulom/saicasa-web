import { Procomiteactaarchivo } from './procomiteactaarchivo';

describe('Procomiteactaarchivo', () => {
  it('should create an instance', () => {
    expect(new Procomiteactaarchivo()).toBeTruthy();
  });
});
